<style>
    table {
        width: 70%;
        margin-left: auto;
        margin-right: auto;
        margin-top: 150px
    }
    table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
        padding: 7px
    }
</style>

<table>
    <tr>
        <th>Date Début</th>
        <th>Date Fin</th>
        <th>Lieu</th>
        <th>Nom</th>
    </tr>
    {% for i in listeInscriptions %}
        <tr>
            <td>{{ i.event.dateD | date }}</td>
            <td>{{ i.event.dateD | date }}</td>
            <td>{{ i.event.lieu }}</td>
            <td>{{ i.event.nom }}</td>
        </tr>
    {% endfor %}
</table>
